en:
  filter_set:
    by:
      text: Text
    submit:
      search:
        caption: "Search %{_scope}"
      export:
        caption: "Export %{_scope} in %{_format} %{_paging}"
        paging: (Current page)
        formats:
          csv: CSV
          excel: Excel
